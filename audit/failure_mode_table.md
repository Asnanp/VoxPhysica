# Failure Mode Table

| rank | failure mode | evidence | intervention |
| --- | --- | --- | --- |
| 1 | Early validation gains are real but do not consolidate | The long-run follow-up `stage3f_height_only_long_stable` hit its best legacy validation/test `4.527 / 5.802` at epoch `5`, then never improved again; early stopping fired after `10` non-improving epochs, and the final train-to-val speaker gap reopened to `+2.309 cm` by epoch `15` | treat early peaks as `hold` only; future changes must improve consolidation and gap control, not just produce one good epoch |
| 2 | Objective/representation quality improved but remains seed-sensitive | Stage 3c replicated to a mixed three-seed result: legacy mean `4.671 / 5.974` vs old frontier `4.611 / 6.157`, with validation stdev `0.284 cm`, speaker gaps near `+2.3 cm` on all seeds, and unstable epoch traces on seeds `17` and `23`; the Stage 3f single-seed gain has not yet replicated | keep the line on hold; do not promote without cleaner multi-seed validation and lower speaker gaps |
| 3 | Hard-slice robustness improves episodically, not stably | In `stage3f`, validation short-height speaker MAE swung `13.48 -> 3.51 -> 21.27 -> 5.94`, and validation quality-medium speaker MAE swung `12.19 -> 7.66 -> 17.74 -> 9.69`; earlier Stage 3c and Stage 3d test short-height slices also remained severe | require slice gains to persist across epochs and seeds before calling them fixed |
| 4 | Weak last-mile pooling is not the breakthrough | The first omega replay on the matched no-physics seed-11 checkpoint lost to legacy pooling (`4.645` vs `4.611` val speaker MAE, `6.312` vs `6.157` test speaker MAE), and even on Stage 3c omega improved val but worsened strict test (`5.970` vs legacy `5.889`) | keep legacy as primary and keep omega diagnostic-only |
| 5 | Speaker batching is not the frontier bottleneck | Stage 2 and Stage 2b both degraded the frontier under strict evaluation, even though they changed speaker structure in opposite directions | stop spending more seeds on batching redesigns unless a new failure mode justifies it |
| 6 | Physics is unearned | No strict multi-seed proof that physics beats the no-physics line | reintroduce physics only as a challenger after the no-physics line improves and replicates |
